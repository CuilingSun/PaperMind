# PRD：PaperMind — CS/AI 论文追踪与深度解析

---

## 版本历史

| 版本 | 日期 | 主要变更 |
|------|------|---------|
| v1.0 | 2026-04-25 | 初始版本：PDF 上传 + 六维结构化分析报告 + Q&A 追问 |
| v1.1 | 2026-04-25 | 中英文 i18n 切换；URL 安全过滤（防 Gemini 输出中文污染链接）；分析加载骨架动画 |
| v1.2 | 2026-04-25 | 新增 arXiv 论文追踪页（`/tracker`）；新增历史记录页（`/history`）；首页重构为 Landing 页 |
| v1.3 | 2026-04-25 | 追踪页关键词自动补全（~130 条 CS/AI 主题词库）；高级筛选面板（作者、arXiv 分类、年份区间、排序） |
| v1.4 | 2026-04-26 | 追踪页改为多关键词 AND 合并搜索（去除分 Tab 设计）；IME 中文输入法兼容；关键词 pill 管理优化 |
| v1.5 | 2026-04-26 | 全局错误边界（`app/error.tsx`）；PDF 下载失败在空状态显示错误条；追踪页错误 + 重试按钮 |

---

## 当前版本：v1.5

**状态**：已实现，持续迭代中

---

### 一、产品定位

**一句话定位**：为 CS/AI 研究者提供的一站式论文工具——追踪 arXiv 最新论文 + 一键深度解析 + 历史记录管理，零注册、零成本。

**核心价值**：
- 用关键词组合订阅 arXiv，自动发现与所有关键词都相关的最新论文
- 上传 PDF 或直接从追踪列表解析，秒级获得 7 维结构化报告
- 支持中英文切换，面向中英文混合阅读场景
- 用户使用自己的免费 Gemini API Key，无使用成本

**竞品对比**：

| | 本产品 | ChatPDF | Notion AI |
|---|---|---|---|
| 需要注册 | 否 | 是 | 是 |
| 结构化报告 | 7 维深度 | 自由问答 | 通用摘要 |
| arXiv 追踪 | ✅ | ✗ | ✗ |
| 解析历史 | ✅ | ✗ | ✗ |
| 费用 | 免费（用户自带 Key） | 有付费墙 | 订阅制 |

---

### 二、目标用户

**主要用户**：CS/AI 方向研究生及科研工作者

**用户画像**：
- 每周精读 3–10 篇英文论文
- 需要持续追踪某几个领域（如 diffusion model、NeRF）的最新进展
- 熟悉 Google AI Studio，能获取免费 Gemini API Key

**核心痛点**：
1. 不知道哪些新论文值得读：每天 arXiv 新论文量大，难以筛选
2. 论文结构复杂，核心贡献难以快速定位
3. 方法细节需要反复阅读才能理解
4. 读过的论文容易遗忘，缺乏可回溯的解析记录

---

### 三、页面结构与用户旅程

```
/            首页（Landing）
├── /tracker 追踪页（arXiv 论文追踪）
├── /analyze 解析页（PDF 深度分析）
└── /history 历史页（解析记录管理）
```

**典型用户旅程**：
1. 首次访问 `/` → 看到两张功能卡片 + 最近 5 条解析历史
2. 进入 `/tracker` → 输入关键词（支持自动补全）→ 可加 Filter → 点击搜索 → 看到与所有关键词相关的论文
3. 在论文卡片点击「解析」→ 新标签页打开 `/analyze`，自动下载并分析
4. 返回 `/tracker` 继续浏览；可随时上传本地 PDF 到 `/analyze` 解析
5. 在 `/analyze` 或 `/history` 查看历史，一键重新解析

---

### 四、功能详情

#### 4.1 首页（`/`）

- 两张功能入口卡片：「追踪 arXiv」→ `/tracker`，「解析论文」→ `/analyze`
- 展示最近 5 条解析历史；超过 5 条时显示「查看全部 →」

#### 4.2 追踪页（`/tracker`）

**关键词管理（KeywordManager 组件）**：
- 关键词输入框，输入时实时显示自动补全建议（~130 条主题词 + arXiv 分类，前缀优先匹配）
- 支持 IME 中文输入法（compositionstart/end 防止中间态提交）
- 从补全列表选中后立即提交；点击「搜索」按钮可手动提交
- 已追踪关键词以 pill 标签显示，点击 `×` 删除单个；「清除全部」一键清空

**高级筛选（Filter 面板，可折叠）**：
- 作者名（文本输入，对应 arXiv `au:` 查询）
- arXiv 分类（下拉，10 个常用 CS/AI 类别）
- 发布年份区间（from / to）
- 排序方式（最新 / 相关度）

**搜索逻辑**：
- 多关键词以 **AND** 合并为单个 arXiv 查询（如 `all:image generation AND all:diffusion model`）
- 每次增删关键词自动重新搜索；错误时显示提示 + 重试按钮

**论文卡片（PaperCard 组件）**：
- 标题、作者、发布日期、摘要（可展开）
- `[arXiv ↗]`、`[PDF ↗]` 链接
- 「解析」按钮：新标签页打开 `/analyze` 自动分析

#### 4.3 解析页（`/analyze`）

**入口方式**：
1. 拖拽 / 点击上传本地 PDF（≤ 20MB）
2. 从 `/tracker` 论文卡片点击「解析」（服务端代理下载 arXiv PDF）
3. 从历史记录点击「再次解析」

**分析报告（7 维，流式输出）**：

| # | 维度 | 内容 |
|---|------|------|
| 0 | **摘要翻译** | Abstract 逐句中文翻译，保留术语 |
| 1 | **方法动机** | 研究驱动力 / 现有方法痛点 / 核心假设 |
| 2 | **方法设计** | Pipeline 概述 / 各模块功能 / 关键公式解释 |
| 3 | **与其他方法对比** | 本质区别 / 创新点 / Markdown 对比表格 |
| 4 | **实验表现与优势** | 数据集 & 指标 / 定量结果 / 消融实验 / 局限性 |
| 5 | **学习与应用** | 复现注意事项 / 关键超参数 / 迁移可能性 |
| 6 | **总结** | 一句话核心思想 + 极简 3–5 步 Pipeline |

- 分析开始时全部 7 个维度框架立即出现，内容逐段流入，未完成维度显示骨架动画
- 中 / 英文随时切换，切换后对未完成语言自动触发新一轮分析
- 报告中"Figure X"可点击，PDF 预览自动跳转对应页
- 报告完成后激活 Q&A 追问（多轮对话，刷新后清空）

**错误处理**：
- API Key 无效 → 提示重新设置
- API 额度耗尽 → 友好提示
- PDF 下载失败 → 上传区顶部红色错误条（可关闭）
- 分析失败 → 报告区显示具体错误

**历史记录**：
- 解析成功后自动写入历史（最多 50 条）
- 未上传文件时展示最近 5 条历史；支持重新解析 / 删除

#### 4.4 历史页（`/history`）

- 全部解析历史，按日期分组（今天 / 昨天 / 具体日期）
- arXiv 论文显示 `[arXiv ↗]` + 「再次解析」按钮
- 单条删除（hover 显示 `×`）；「清除全部」带 confirm 确认

---

### 五、技术架构

**技术栈**：Next.js 14 App Router + TypeScript + Tailwind CSS + Gemini API + Vercel

**目录结构**：
```
/app
  page.tsx                    # 首页（Landing）
  error.tsx                   # 全局错误边界
  /analyze/page.tsx           # 解析页
  /tracker/page.tsx           # 追踪页
  /history/page.tsx           # 历史页
  /api/arxiv/route.ts         # arXiv 搜索代理（CORS bypass）
  /api/arxiv-pdf/route.ts     # arXiv PDF 下载代理

/components
  NavHeader.tsx               # 共享导航栏（含语言切换）
  ApiKeyModal.tsx             # API Key 输入弹窗
  PdfUpload.tsx               # 拖拽上传区
  PdfViewer.tsx               # PDF 预览（动态加载，SSR=false）
  ReportView.tsx              # 7 维报告 + 骨架动画 + 错误展示
  ChatPanel.tsx               # Q&A 多轮对话
  KeywordManager.tsx          # 关键词输入 + Filter 面板 + pills
  PaperCard.tsx               # arXiv 论文卡片
  HistoryList.tsx             # 历史记录列表
  LangDropdown.tsx            # 语言切换下拉

/lib
  gemini.ts                   # Gemini SDK 封装
  prompts.ts                  # 分析 & 对话 Prompt 模板（中英文）
  arxiv.ts                    # arXiv 类型定义 + 搜索函数
  arxivSuggestions.ts         # 关键词自动补全词库
  history.ts                  # 历史记录 CRUD（localStorage）
  figureParser.ts             # 解析报告中的图表引用
```

**Gemini 模型**：
- 论文分析：`gemini-3.1-flash-lite-preview`（流式，原生 PDF 输入）
- Q&A 追问：`gemini-2.5-flash-lite`（流式多轮对话）

**数据持久化**（全部 localStorage）：

| Key | 内容 |
|-----|------|
| `gemini-api-key` | 用户 API Key |
| `arxiv-keywords` | 追踪关键词列表（`TrackedSearch[]` JSON） |
| `paper-history` | 解析历史（`HistoryEntry[]`，上限 50 条） |
| `pending-arxiv-id` | 跨标签传递待解析论文 ID |
| `pending-arxiv-title` | 跨标签传递待解析论文标题 |

---

### 六、非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 流式分析首字节 < 5s；完整报告 < 60s；首屏 < 2s |
| 安全 | API Key 仅存本地，不经过应用服务器；PDF 不持久化 |
| 可靠性 | API 失败有错误 + 重试；全局 React 错误边界防白屏 |
| 兼容性 | Chrome / Firefox / Safari 最新两版本；最小视口 1024px；支持中文 IME |

---

### 七、已知限制 & 未来方向

| 类型 | 描述 |
|------|------|
| 当前限制 | 移动端未适配 |
| 当前限制 | 历史记录仅存本地，换设备后丢失 |
| 当前限制 | arXiv 搜索结果上限 20 条 |
| 未来方向 | 报告导出为 Markdown |
| 未来方向 | BibTeX 引用一键生成 |
| 未来方向 | 追踪页支持论文收藏（标星） |
| 未来方向 | 多论文对比分析 |

---

---

## 历史版本存档

---

### v1.0（2026-04-25）— 初始版本

**状态**：已确认

#### 一、产品定位

**一句话定位**：为 CS/AI 研究者提供的一键式论文结构化分析工具——上传 PDF，秒级获得摘要翻译 + 六维深度报告，并可自由追问。

**核心价值**：
- 零配置、无需注册，立即可用
- 结构化输出对应研究者最关心的六个维度
- 用户使用自己的免费 Gemini API Key，无使用成本

**竞品对比**：

| | 本产品 | ChatPDF | Notion AI |
|---|---|---|---|
| 需要注册 | 否 | 是 | 是 |
| 结构化报告 | 六维深度 | 自由问答 | 通用摘要 |
| 费用 | 免费（用户自带 Key） | 有付费墙 | 订阅制 |
| 面向人群 | CS/AI 研究者 | 通用 | 通用 |

#### 二、目标用户

**主要用户**：CS/AI 方向研究生及科研工作者

**用户画像**：
- 每周精读 3–10 篇英文论文
- 有一定英文阅读能力，但希望降低认知负担
- 熟悉 Google AI Studio，能获取免费 Gemini API Key

**核心痛点**：
1. 论文结构复杂，核心贡献难以快速定位
2. 方法细节理解需要反复阅读
3. 与已有工作的对比依赖大量背景知识
4. 阅读完无法形成清晰的记忆锚点

#### 三、用户故事

**US-01（核心）：一键生成分析报告**
> 作为研究生，我上传一篇论文 PDF，希望自动获得结构化分析报告，  
> 这样我可以快速判断这篇论文是否值得精读，以及重点精读哪些部分。

验收标准：
- 上传 PDF 后自动触发分析，无需额外操作
- 先输出摘要中文翻译，再依次呈现六个维度
- 所有输出为中文（论文为英文时自动翻译）
- 流式输出，首字节延迟 < 5 秒

**US-02（核心）：基于论文的自由追问**
> 作为研究者，我阅读报告时对某个方法细节存疑，希望直接用中文提问，  
> 这样无需反复翻阅原文即可获得针对性解答。

验收标准：
- 报告生成后 Q&A 输入框自动激活
- AI 回答严格基于当前论文内容，不编造
- 支持多轮对话；刷新后会话清空

**US-03（配置）：填写 Gemini API Key**
> 作为首次使用的用户，我希望在清晰的入口填入我的 API Key。

验收标准：
- 首次访问有明显的 Key 输入引导弹窗
- Key 存储在 localStorage，下次访问自动填充
- 页面内提供获取免费 Key 的跳转链接（Google AI Studio）
- Key 格式错误或无效时给出友好提示

#### 四、功能范围

**P0 — MVP 必须完成**

| 功能 | 描述 |
|------|------|
| PDF 上传 | 支持拖拽和点击选择；格式限 PDF；大小限 ≤ 20MB |
| 摘要翻译 | 提取 Abstract 并输出准确中文翻译（保留术语） |
| 六维结构化报告 | 流式逐维度展示 |
| Q&A 追问 | 基于当前论文的多轮中文对话 |
| API Key 管理 | 引导输入 → localStorage 持久化 → 支持清除 |
| 错误处理 | API 超限 / Key 无效 / 文件格式错误的友好提示 |

**明确不做（v1.0）**：
- 用户登录与账号系统
- 跨会话历史记录持久化（→ v1.2 已实现）
- 多文件同时对比分析
- 移动端适配

#### 五、分析报告结构（v1.0）

前置：摘要翻译（Abstract 逐句翻译）

| # | 维度 | 分析子项 |
|---|------|---------|
| 1 | **方法动机** | 研究驱动力 / 现有方法痛点 / 核心研究假设 |
| 2 | **方法设计** | 整体 Pipeline / 各模块功能 / 关键公式中文解释 |
| 3 | **与其他方法对比** | 与 baseline 的本质区别 / 核心创新点 / Markdown 对比表格 |
| 4 | **实验表现与优势** | 实验设计 / 关键定量结果 / 消融实验 / 局限性 |
| 5 | **学习与应用** | 复现注意事项 / 关键超参数 / 方法迁移可能性 |
| 6 | **总结** | 一句话核心思想 / 速记版 Pipeline（3–5 步） |

#### 六、技术架构（v1.0）

**技术栈**：Next.js 14 App Router + TypeScript + Tailwind CSS + Gemini API + Vercel

**目录结构**：
```
/app
  page.tsx              # 主页面
  /api/analyze/route.ts # PDF 分析接口（流式）
  /api/chat/route.ts    # Q&A 追问接口（流式）
/components
  ApiKeyModal.tsx
  PdfUpload.tsx
  ReportView.tsx
  ChatPanel.tsx
/lib
  gemini.ts
  prompts.ts
```

**Gemini 模型**：`gemini-3.1-flash`（免费版，支持 PDF 原生输入）

#### 七、成功指标（v1.0）

| 指标 | 上线后 4 周目标 |
|------|--------------|
| 周独立访客数 | ≥ 200 |
| PDF 上传转化率 | ≥ 60% |
| Q&A 使用率 | ≥ 40% |
| 平均追问轮次 | ≥ 2 轮 |
| 报告生成成功率 | ≥ 95% |
